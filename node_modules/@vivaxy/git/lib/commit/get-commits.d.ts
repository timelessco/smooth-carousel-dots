import { FileStatus } from '../helpers/types';
export interface Commit {
    message: string;
    hash: string;
    tags: string[];
    date: number;
    files: FileStatus[];
}
export default function getCommits({ from, to, paths, noMerges, cwd, }: {
    from?: string;
    to?: string;
    paths?: string[];
    noMerges?: boolean;
    cwd: string;
}): Promise<Commit[]>;
