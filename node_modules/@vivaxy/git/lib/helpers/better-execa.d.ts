/**
 * @since 2019-07-25 16:10
 * @author vivaxy
 */
import * as execa from 'execa';
import { Stdio } from './types';
export default function betterExeca(bin: string, commands: string[], { cwd, shell, stdio, }: {
    cwd: string;
    shell?: boolean;
    stdio?: Stdio;
}): Promise<execa.ExecaReturnValue<string>>;
