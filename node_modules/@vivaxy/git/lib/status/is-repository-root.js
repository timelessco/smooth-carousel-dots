"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @since 2016-11-22 16:05
 * @author vivaxy
 */
const path = require("path");
const fse = require("fs-extra");
const is_inside_work_tree_1 = require("./is-inside-work-tree");
async function isRepositoryRoot({ cwd, }) {
    if ((await fse.pathExists(path.join(cwd, '.git'))) &&
        (await is_inside_work_tree_1.default({ cwd }))) {
        return true;
    }
    return false;
}
exports.default = isRepositoryRoot;
