import { Stdio } from '../helpers';
export default function push({ cwd, setUpstream, followTags, stdio, }: {
    cwd: string;
    setUpstream?: boolean;
    followTags?: boolean;
    stdio?: Stdio;
}): Promise<void>;
