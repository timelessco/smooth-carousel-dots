"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @since 2019-06-27 10:43
 * @author vivaxy
 */
const helpers_1 = require("../helpers");
async function getLastTag({ match = '*', from = 'HEAD', cwd, }) {
    try {
        const { stdout } = await helpers_1.betterExeca('git', ['describe', '--match', `"${match}"`, '--abbrev=0', '--tags', from], 
        // TODO: why shell needs to be true?
        { cwd, shell: true });
        return stdout;
    }
    catch (e) {
        return '';
    }
}
exports.default = getLastTag;
