"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @since 20190920 16:46
 * @author vivaxy
 */
const helpers_1 = require("../helpers");
async function getTags({ match = '*', sort = 'refname', cwd, }) {
    try {
        const { stdout } = await helpers_1.betterExeca('git', ['tag', '--list', match, `--sort=${sort}`], { cwd });
        return stdout.split('\n').filter(Boolean);
    }
    catch (e) {
        return [];
    }
}
exports.default = getTags;
