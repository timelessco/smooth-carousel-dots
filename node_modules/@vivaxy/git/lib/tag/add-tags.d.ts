/**
 * @since 2019-06-28 10:07
 * @author vivaxy
 */
import { Stdio } from '../helpers';
export default function addTags(tags: {
    tag: string;
    message: string;
}[], { cwd, stdio }: {
    cwd: string;
    stdio?: Stdio;
}): Promise<void>;
