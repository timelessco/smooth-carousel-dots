import { EMOJI_TYPES } from './configs';
declare type Hooks = {
    postpush: string;
};
export default function gacp({ cwd, add, push, emoji, editor, hooks, }: {
    cwd: string;
    add: boolean;
    push: boolean;
    emoji: EMOJI_TYPES;
    editor: boolean;
    hooks: Hooks;
}): Promise<void>;
export {};
